# CLAUDE.md

家計・税金管理 Web アプリ。既存 Excel（`sample-file.xlsx`）の後継となる、**ローカル単一ユーザー専用**の試算ツール。要件の全文は [REQUIREMENTS.md](REQUIREMENTS.md) を正とする。本ファイルは以降のセッションが最初に読む前提知識・規約をまとめたもの。

## このプロジェクトの大原則

- **`sample-file.xlsx` の `税金算出` シートの数式が税額ロジックの source of truth**。法的正確性より「Excel の再現」を優先する（REQUIREMENTS.md §1）。数式に迷ったら Excel を開いて確認する。
- **確定申告本体はマネーフォワードで行う**。本アプリは計画・試算用。
- **税率・控除額・基礎控除・青色控除・均等割等はコードにハードコードしない**。年度別マスタとして持ち、標準表からの自動判定を基本に、**年度ごとに手動上書き可能**にする（税制は毎年変わる／Excel が手入力値を使っているため）。
- 各ステップで「動く状態」を保ちながら小さくコミットする。

## 技術スタック

| 層 | 採用 | 補足 |
|---|---|---|
| バックエンド | Python 3.11+ / FastAPI | API＋計算ロジック |
| データ | SQLite（単一ファイル） | ORM は SQLAlchemy 2.0、マイグレーションは Alembic |
| フロント | Jinja2 + HTMX | サーバ側で部分HTML(fragment)を返し、フォーム送信で即時に集計を再描画 |
| 起動 | uvicorn、`127.0.0.1` バインド | localhost のみ。`0.0.0.0` にしない。認証なし |
| テスト | pytest | 税額モジュールの検証が最優先 |

> DB アクセスを raw `sqlite3` に寄せたい等、方針変更が要る場合はコードを書く前に相談する。

## ディレクトリ構成

```
tax-life-calculation/
├── CLAUDE.md
├── REQUIREMENTS.md
├── sample-file.xlsx              # 税額ロジックの source of truth
├── pyproject.toml / requirements.txt
├── .gitignore                    # data/*.db・実金額を含むJSON を必ず除外
├── app/
│   ├── main.py                   # FastAPI 生成・ルータ登録・uvicorn エントリ
│   ├── config.py                 # 設定（DBパス・ポート等）
│   ├── db.py                     # エンジン/セッション
│   ├── models.py                 # SQLAlchemy モデル
│   ├── schemas.py                # Pydantic
│   ├── core/
│   │   ├── tax.py                # ★税額計算（純粋関数・副作用なし）= 最重要
│   │   └── living.py             # 生活費集計（純粋関数）
│   ├── routers/
│   │   ├── tax.py                # /tax 画面・API
│   │   └── living.py             # /living 画面・API
│   ├── templates/                # Jinja2（base.html + tax/ living/ + fragments/）
│   └── static/
├── data/
│   ├── masters/                  # 年度別マスタの seed（税率表・給与所得控除表 等, JSON or SQL）
│   └── app.db                    # 実データ。gitignore
├── migrations/                   # Alembic
├── tests/
│   ├── test_tax.py               # REQUIREMENTS.md §4 検証表
│   └── test_living.py
└── scripts/
    ├── import_excel.py           # 既存Excelからの初期データ取り込み（任意）
    └── backup.py                 # DBバックアップ
```

## 起動・開発コマンド（Windows / PowerShell）

```powershell
# 初回セットアップ
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt

# マイグレーション適用（Alembic 導入後）
alembic upgrade head

# 開発サーバ（localhost のみ）
uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload

# テスト（税額の検証が通ることを最優先で確認）
pytest -q
pytest tests/test_tax.py -q
```

## コーディング方針

### 金額と単位（最重要の実装規約）
- **生活費収支は 円（整数、10円単位）** で保持・集計する。入力は `step=10`、10円未満は丸め or 拒否。
- **税金算出は Excel が万円(float)基準**。`core/tax.py` は **万円(float)で計算して Excel を厳密再現**し、円⇔万円の変換は境界（ルータ/表示層）で行う。税金画面は万円表示を基本とし、円表示も選べると尚可。
- 検証時の突合は Excel 実測に合わせ、小数第2位程度の許容誤差で比較する（float 演算のため厳密等価にしない）。

### 税額計算（`core/tax.py`）— Excel を正とする
純粋関数として実装する。`税金算出` シートの数式（検算済み。全て単位=万円）:

```
所得金額         = 事業所得 + 給与所得 − 経費 − 青色申告控除            # C7 = B9+C9-D9-K9
寄付金控除(base) = max(0, 寄付金 − 0.2万)                               # 自己負担2000円=0.2万を除いた額
課税所得金額     = 所得金額 − (基礎+配偶者特別+生命保険+社会保険
                              +小規模企業共済+その他所得控除+地震保険) − 寄付金控除(base)  # F7 相当 − ふるさと
                 ※青色申告控除は所得金額で控除済みのため課税所得では差し引かない（+K9 で戻す）
                 ※ふるさと納税(寄付金)は base を課税所得から差し引く（＝所得税・住民税基本分の軽減はここで反映。二重計上しない）
所得税額(控除前) = 課税所得金額 × 税率 − 控除額                         # O7 相当（＝income_tax_before_credits）
所得税額         = max(0, 所得税額(控除前) − 住宅ローン控除)             # 住宅ローンは税額控除(0可)
復興特別所得税   = 所得税額 × 2.1%（控除後の所得税額が基準）             # Q7 相当
住民税           = max(0, 課税所得金額 × 10% − 住民税控除) − ふるさと特例分  # 特例分 = base × (0.9 − 税率)
市県民税(均等割) = 0.5万 固定                                          # U7
消費税           = 所得金額 × 5%（簡易見積り。方式切替可能に）          # W7 = C7*0.05
税金合計         = 所得税+復興+住民税+市県民税+消費税                   # O6
税金及び経費支払い後残金 = 事業所得 − 経費 − 税金合計                    # （旧「手取り合計」を再定義）
社会保険料支払後合計     = 上記 − 社会保険控除                          # （旧「各種支払後残金」を再定義）
ふるさと納税上限 = 課税所得金額(寄付金控除前) × 0.2 ÷ 7                  # 自身の寄付で縮まないよう控除前を基準
所得税分割 = 所得税÷5 / 住民税分割 = 住民税÷4 / 消費税分割 = 消費税÷5  # R6,S6,W6（控除後の税額を分割。分割数は可変）
```

> 住宅ローン控除（税額控除・0 可）、ふるさと納税（寄付金 base を**課税所得から控除**し所得税・住民税基本分を反映＋住民税特例分は税額控除）、および出力2項目の再定義はユーザー指示による Excel からの意図的な拡張。**REQUIREMENTS.md §4 の 課税所得747.7・89.54 は「寄付金控除前」の骨格**を指す（テストは `taxable_income_before_furusato` / `income_tax_before_credits` で検証）。

Excel から確定した重要な注意点:
- **所得税の税率・控除額は年度別の手入力値**（2025 は税率20%/控除60万。標準の累進表なら課税所得747.7万は23%/63.6万の区分だが、Excel は手入力値を使い期待値89.54万になる）。→ **標準累進表からの自動判定を既定に、年度別マスタで手動上書き可能**な設計にする（REQUIREMENTS.md §5-1）。標準表は REQUIREMENTS.md §4 の7区分。
- **「その他所得控除」は Excel の列J（ラベルなし列）**。値: 2025=0 / 2026=81.6 / 2027=90。所得控除として拾う。
- 基礎控除は年度パラメータ（2025=58万）。復興税率・住民税率・均等割も年度パラメータ化する。
- 消費税は簡易見積り。**算定方式を切替可能**にしておく（インボイス2割特例等の将来対応）。

### 年度別マスタ
- テーブル/マスタで持つもの: 累進税率表（区分・税率・控除額）、給与所得控除表、青色申告控除額(55/65/75)、基礎控除、配偶者特別控除、復興税率、住民税率、均等割、消費税率/方式、税額分割数。
- 各税年度の「入力」と「使用パラメータ（自動判定 or 手動上書き）」を分けて保存し、Excel の年度シート（2025/2026/2027）を再現できること。

### HTMX / 画面
- 集計はサーバ側計算を正とし、`hx-post` → 部分テンプレート(fragment)差し替えで即時更新。クライアント側で税額を再計算しない。

### データ保全 / Git
- **実データ（`data/*.db`、実金額を含む JSON）は必ず `.gitignore`**。コード・スキーマ・マスタ定義のみコミット可。
- CSV / Excel エクスポートを用意（マネーフォワード・従来Excelとの突合用）。手動バックアップボタンも設ける。

## 実装の進め方（REQUIREMENTS.md §6 の順・厳守）

1. **CLAUDE.md 作成**（本ファイル）← 完了。
2. **スキーマ設計**: 銀行 / 費目カテゴリ / 月次エントリ / 税年度パラメータ（マスタ）/ 税年度入力 のテーブル設計。年度別パラメータをマスタ化。
3. **税額算出モジュール（先に作る）**: 上記ロジックを `core/tax.py` に純粋関数で実装 → **REQUIREMENTS.md §4 の検証表でユニットテスト**。下表に一致するまで次に進まない。
4. **税金算出 画面**: 入力フォーム＋結果表示。
5. **生活費収支 モジュール＋画面**: 銀行×カテゴリ×月次の入力と集計。税金貯金は税額分割と連動。
6. **CSV/Excel エクスポート・バックアップ・既存Excel取り込み（任意）**。

### 到達点の検証（§3 完了条件・2025年入力／単位=万円）
入力: 事業所得1050 / 給与0 / 経費10 / 青色65（基礎58・生命7.2・社会76.8・小規模84・その他0・地震1.3・寄付12、手入力税率0.2/控除60）

| 項目 | 期待値 |
|---|---|
| 所得金額 | 975 |
| 課税所得金額 | 747.7 |
| 所得税額 | 89.54 |
| 復興特別所得税 | 1.88 |
| 住民税 | 74.77 |
| 消費税 | 48.75 |
| 税金合計 | 215.44 |
| 手取り合計 | 747.76 |
| ふるさと納税上限額 | 21.36 |

## 参考: 生活費算出シートの構造（機能A）
- 各月エントリは「銀行 × 費目カテゴリ × 年月 × 金額」で保持すると集計が柔軟。
- 銀行例: 楽天銀行 / 三井住友 / りそな / UFJ / 現金。費目例: 食費・光熱費(電気/ガス/水道)・保険(生命/国民健康保険/地震)・国民年金・くもん・NHK・小遣い・各種返済 等（階層可・編集/削除/並び替え可）。
- **貯蓄(kind=saving)は2グループに分割**: 税金貯金(所得税/住民税/消費税/固定資産税) と 積立貯金(iDeco/小規模企業共済/NISA/年金保険/現金貯蓄)。月次集計で「うち税金貯金／うち積立貯金」を分けて表示。
- 月次で 収入総額 / 支払総額 / 余剰金、銀行ごとの内訳合計、年次サマリを表示。税金貯金列は税額分割と連動させる。
